# 初めてのAKB48ガイド(非公式ファンサイト)

AKB48の劇場公演・握手会に初めて行く人向けの、非公式ファンサイト。HTML / CSS / JavaScript だけで作っていて、ビルドは不要。

## ユーザーについて
- プログラミング未経験、Claude Code は初めて。日本語で、専門用語は言い換えて、やさしく説明する。
- 手をなるべく動かさず、Claude が自律的に進めるのを望んでいる。ただし、アカウント登録・パスワード入力・セキュリティ設定の変更は、ユーザー本人にやってもらう。
- ユーザーの呼び名は「ぱちゃ」。Notion 上に、このサイト以外の構想(事業構想・ロードマップなど)もあり、Cowork の Claude(Notion 側)と、この Claude Code(PC側)とで、役割が分かれている。

## Notionの開発ログ(作業の区切りごとに追記)
- ページ: 「📝 開発ログ(Claude Codeが書く)」(Notion。サイト拡張ロードマップの子ページ)。Cowork の Claude が、ここを読んで進捗を把握する。
- **区切り(だいたい1PRごと)のたびに、このページの一番上に、次の形式で1件追記する**(新しいものが上):
  ```
  ## YYYY-MM-DD　タイトル(PR #番号があれば)
  **やったこと**
  **変更したページ**
  **プレビューURL**(あれば。`https://deploy-preview-<PR番号>--akb-hajimete-guide.netlify.app/`)
  **次にやること**
  **ぱちゃに確認したいこと**
  ```
- 追記は、Notion MCP(`notion-update-page` の `update_content`)で行う。既存のログを消さず、新しい entry を先頭に足す。
- 「マージ完了」の報告や、大きな作業の切れ目のたびに書く。細かい調査・修正の1つ1つには、書かなくてよい。

## ファイル
サイト本体は `site/` の中だけ(ここだけが公開される)。`CLAUDE.md` `README.md` `netlify.toml` は公開されない。
- `site/index.html` `theater.html` `handshake.html` `planner.html`: 各ページ
- `site/style.css`: 全ページ共通の見た目(色は先頭の `:root` で変える。ピンク基調)
- `site/planner.js`: 部割プランナーの計算。データは `site/data/events.js` にあり、新しい握手会は `EVENTS` に同じ形で足すだけ

## 部割データの方針
- 新しい握手会の部割が公式に出たら、`EVENTS` に足す(新しいものを先に置く)。**終わった握手会のデータは、消さずに残し、`status` を「開催終了」にする**(2026-09-22 に、「終わったら消す」から変更)。プランナーの選択で、過去の部割も見られる。
- 日付・公式リンク・過去の公演一覧への掲載は、`site/data/schedule.js` で管理する(下の「予定と過去の公演」)。
- 握手会の種類は `type` で区別する。プランナーが対応するのは `個別握手会` だけ(メンバーと部を選んで参加権利を取る形式)。初回限定盤の全国握手会は、まとめ出し・グループ握手会・部ごとの参加メンバーなど、仕組みが違うので、いまは対応しない。
- 更新の流れ: 公式の部割(画像・PDF)を読み取る → `site/data/events.js` を入れ替える → ユーザーに確認してもらう → PR → プレビュー確認 → ユーザーがマージ。
## 予定と過去の公演(`site/data/schedule.js` / `site/schedule.js` / `site/past.html`)
- イベントは `SCHEDULE` に足す(形と決まりは、ファイルの先頭に書いてある)。**終わったものは消さない。** 「直近の予定」(トップ・ライブ・握手会)には、まだ終わっていないものだけが並び、「過去の公演」には、終わったものが新しい順に並ぶ。
- 終了の決まり: `ended: true`(手で終わらせる)→ `endsAt`(終了の日時)→ `lastDay`(最終日が終わったら)。**終了日時が不明なら、開始時刻を過ぎただけで終了にしない**(`lastDay` が、その日が終わるまで待つ)。見た人の端末の時計で判定する。
- **まだ開催されていないイベントを、動作確認のために `ended: true` にして公開しない。** 確認は、`applySchedule(時刻)` に未来の時刻を渡して、画面の中だけで行う(ファイルは変えない)。
- ガイドのページの部品: `<div data-event-status="ID">`(終了・開催中の帯)、`data-active-only="ID"`(終了したら隠す。「販売中」の表示・購入リンク)、`data-ended-only="ID" hidden`(終了したら出す)。ID は `SCHEDULE` の `id` と同じにする。JavaScript が動かないときは、ページに書いた内容が、そのまま見える。
- 終了後も続くもの(例: Hulu の見逃し配信は10月27日まで)は、隠さない。帯にも、そう書いてある。
- 既存のURL・ページ内リンク(`live.html#three` など)は、変えない。ページや節を動かすときは、以前のリンクからも着けるようにする(リダイレクトか、古いIDの残し)。
- 公演が終わったら: セットリストが確かな情報で確認できたら、`#setlist` に載せて、`setlist.published` を `true` にする。感想へのリンク(`voices`)は、`report.html#voices`。
- **トップの「今週末はライブ」帯**(`index.html` の `.week-banner`)も、この `data-active-only` / `data-ended-only` の仕組みで、日付が来たら自動で切り替わる(切り替えのために、あとからマージし直す必要がない)。`three-concepts-live` には、`endsAt`(2026-09-27 22:00。終演時刻は未発表なので、あくまで内部の切り替え用の目安で、ページには表示しない)を設定してある。次のライブのときは、`SCHEDULE` に新しいイベントを足し、`index.html` の帯の文言と `data-active-only`/`data-ended-only` の対象IDを、そのイベントのIDに書き換える。

## 公演後の「空の器」(2026-09-22、金曜9/25マージ分の下書きより)
- **セットリスト**: `live.html#setlist` に、公演ごとの `<details class="spoiler-box">` を3つ用意済み(ネタバレ防止の折りたたみ)。公演後、確認でき次第、それぞれの中の `<p class="pending">` を、実際のセットリストに書き換える。
- **初参加者の声まとめ**: 新しいページは作らず、`report.html` の絞り込み(`whoFilter = "first"`)への直リンク `report.html#first` を用意した(`report.js` が hash を見て、最初から絞り込む)。`live.html` や、今後のページから、この形でリンクできる。
- **Kアリーナ実録**: `site/k-arena-report.html`(新規)。見出しだけの「空の器」(駅→会場/入場/席まで/帰り道/持っていってよかった物)。`<meta name="robots" content="noindex">` を付け、**どこからもリンクしていない**(nav・index・sitemap.xmlに入れていない)。公演後、実体験の中身が入ってから、`venues.html` などからリンクして公開する。sitemap.xml に足すのも、そのとき。

## ライブの予習ガイド(`site/preview.html`)
- 新曲の公式MVと、よく知られている曲の公式MV(押すまで読み込まない。`video.js`)、コールの説明。**セットリストの予想ではない**(「歌われる」とは書かない)。曲の映像は、YouTube の oEmbed で、公式チャンネル(author_name が「AKB48」)であることと、埋め込み可能なことを確認してから足す。2026-09-22 に確認: ヘビーローテーション・大声ダイヤモンド・君のことが好きだから(14th『RIVER』のカップリング)。「会いたかった」の【MV full】は、公式ではない別のチャンネルの投稿だったので、載せていない。**コールがない曲(恋するフォーチュンクッキー・365日の紙飛行機)は、ユーザーの指示で外した**(予習は、コールのある有名曲が中心)。
- 「よく知られている曲」の選び方は、運営者(ユーザー)が決める。曲を足すときは、上の確認を必ず行う。
## 握手・会話の時間の目安(`handshake.html` `national.html` `handshakes.html`)
- 公式の案内(66th・68th)には、1枚あたりの秒数は書かれていない。書かれているのはまとめ出しの枚数の上限(開始〜20分後は3枚まで、20分後〜受付終了は30枚まで、その後は制限なし)。
- 個別握手会: ネット上の個人の書き込み(Yahoo!知恵袋、2019年・2023年)にある「1枚10秒の設定、実際は5〜7秒」を、ラベル「ファンの声」で載せた。ユーザーが「だいたい合っている」と確認済み(2026-09-22)。2010年の古い回答(2〜5秒)は使っていない。
- 全国握手会: 最近のネットの情報は見つからなかった。ユーザーの体験「5秒くらいの感覚」を、ラベル「運営者の体験」で載せた。
- 数字は、公式ではなく目安と明記する。新しい情報が出たら見直す。

## 遠征ガイド(`site/venues.html`)
- 会場ごとの、行き方・入退場の注意・宿泊の探し方・日程の考え方。いまは、Kアリーナ横浜(2026-09-26/27)と幕張メッセ(2026-10-04)。公式のアクセスページ・来場のお願いを元に、2026-09-22 に確認した。会場を足すときは、公式のアクセスページで確認する。
- **ホテルの料金・空室は載せない**(毎日変わる)。探し方と、公式のリンクだけ。
- Kアリーナ横浜のロッカー(400〜800円・最大6,240個)、クローク(K-Arena Shop・1,000円・終演後1時間以内に受け取り)、キャッシュレスのみ・自販機なし、は Kアリーナ横浜の公式FAQ(2026-09-22 確認)より。「駅から会場までの道」「帰りの混雑」の実体験は、公演後に追記予定(リンク先は用意しない。中身が入ってから)。
- **収益化(アフィリエイト)は、まだ入れない**(ユーザーの方針)。将来、宿泊・交通の予約リンクを入れるときは、広告であることをはっきり表示し(ステマ規制)、その部分を、公式の情報と見分けられるようにする。
- 終わった会場の節は、消さずに残す(「過去の公演」の方針と同じ)。

## 開場・開演時刻などのデータの一元化(未着手)
- Notion の「🎤 今週のライブ対応｜金曜 9/25 マージ分」(https://app.notion.com/p/3e346786befe8118aba3c26698bc8af8 )のグループ4。開場・開演などの時刻が、`live.html` の複数箇所(表・`#day`・`#summary`)に、同じ内容が別々に書かれている。1か所にまとめて、ほかは参照する形にできると理想的。2026-09-22 時点、時間の都合で未着手(ユーザー了承済み)。着手するときは、`data/schedule.js` の各イベントに `times` のような項目を足して、そこから表示を組み立てる案が考えられる。

## 収益化・広告(アフィリエイト)の方針
- **正本は Notion の「💰 収益化方針メモ|アフィリエイト」**(https://app.notion.com/p/3e346786befe81c9aa38c37640cbc688 )。ここには要点だけ書く。迷ったら、まず Notion のメモを読み直す。
- **今は導入しない。** 広告リンク・紹介リンク(アフィリエイトリンク)は、いまは1つも足さない。
- **公式の購入導線(Official Shop盤・チケットセンターなど)には、将来も紹介料をつけない。** 常に、公式そのままのリンクにする。
- **将来、紹介リンクを入れるページは、必ず冒頭に「PR」と明記する**(ステマ規制対応)。
- **AKBと無関係な高単価案件(クレカ・格安SIM・脱毛など)は扱わない。**
- 導入候補(優先順・Notionのメモより): 遠征の宿・交通 → 現場の持ち物 → 通常盤CD等(条件つき) → OS盤の買取・処分サービス(将来の本命)。
- 導入する前に、まず GA4 で「どのページで外部リンクの需要があるか」を計測してから判断する(下記)。

## アクセス解析(`site/analytics.js`)
- GA4(Googleアナリティクス)を使うための、読み込みの「場所」だけ用意してある(2026-09-22)。全16ページの `</body>` 直前で読み込む。
- `GA_MEASUREMENT_ID` が空("")のあいだは、何もしない(Googleへの通信は発生しない)。**GA4のアカウント作成・測定IDの発行は、ぱちゃ本人が行う**(Claude はアカウントを作らない)。IDが入ったら、外部サイトへのリンクのクリックを `outbound_click` イベントとして送る(個人を特定できる情報は送らない)。
- 測定IDを入れて有効にしたら、`about.html` の GA4 の説明(「まだ有効にしていません」の部分)を、実際の状態に合わせて更新する。

## 卒業メンバーの「その後」のコンテンツ(案。まだ作っていない)
- ユーザーの案(2026-09-22): 卒業メンバーの近況・SNSアカウントを載せると、昔好きだった人に刺さる。作る前に、プライバシー・肖像権・本人の意向・誤情報のリスクを整理して、ユーザーと相談する。作るなら、本人・所属事務所が公式に公表している範囲のリンクだけにし、削除の依頼を優先して受ける。

## 参加ガイドの「費用・当日の流れ・持ち物」(`#summary`)
- `live.html` `handshakes.html`(種類の比較)`handshake.html` `national.html` `theater.html` に、`#summary` の節がある(2026-09-22 に追加)。
- **書き方の決まり**: 費用は「必ずかかるもの」と「任意・条件・人によって変わるもの」を分ける。金額の例は、対象のイベントと、計算の前提、含まない費用を書く(根拠のない金額は作らない)。当日の流れでは、開場・開演・受付(締め切り)を区別する。公式・ファンの声・このサイトの提案は、ラベル(`.ex` `.ex.fan` `.ex.mine`)で分ける。確認できない項目は、「記載が見当たりません」「確認できていません」と書く(埋めない)。
- **イベントごとのルールを、AKB全体の共通ルールとして書かない**(本人確認書類・電子チケット・ペンライトなど)。対象のイベントを、節の先頭に書く。
- 確認した公式の情報(2026-09-22): 68thシングルは、初回限定盤2,000円(TYPE-A/B/C)・通常盤1,200円・Official Shop盤1,200円(税込。「収録内容のお知らせ」と「初回限定盤の握手会の案内」で一致)。Official Shop盤1枚に付く参加権利の数と、送料・手数料は、公式の案内で確認できていない。THREE CONCEPTS LIVE のチケット代のほかの手数料は、公式の案内に記載なし。Hulu は、単公演券3,900円・3公演セット7,500円、見逃し配信は10月27日 23:59まで。
- 運営者の現場の体験は、ラベル `.ex.owner`(運営者の体験)で載せる。2026-09-22 に聞いた内容: ひとり参加でも全く浮かず、ひとりで来ている女性も多い/コールを知らなくても楽しめるが、有名な曲は予習すると倍楽しめる(`live.html#solo`、ライブでの体験として載せた。握手会・劇場にも当てはまるかは未確認)。
- 新しいシングル・ライブが出たら、対象のイベントの節ごと見直す。ユーザーの現場の経験が必要な項目(ひとり参加の雰囲気、コールを知らなくても楽しめるかなど)は、作らず、質問して集める。

## トップページ(`site/index.html`)
- 先頭は「まだ何に行きたいか決まっていない人へ」(ライブ・握手会/オンラインお話し会・劇場公演の比較。何を楽しめるか・参加までの流れ・費用の例)。その下が「何を知りたいですか?」(ほかの入口)、「直近の予定」、不安なことのリスト、更新情報(古いものは折りたたみ)。
- 「どれか1つ」に断定しない(全国握手会はライブつき、劇場でもガラポンでメンバーと写真、など、重なる部分がある)。

## 申し込みガイド(`site/apply.html`)
- 申し込み方法はシングルごとに変わる。シングル固有のURL(申込ページ、操作ガイド)は「68thシングルの例」と明記し、次のシングルが出たら見直す。
- 収益化・広告の方針は、上の「収益化・広告(アフィリエイト)の方針」を見る。公式の購入導線には、紹介料をつけない。

## 全国握手会ガイド(`site/national.html`)
- 初回限定盤の握手会(全国握手会)のガイド。2026-09-21 に、福岡握手会(開催済み)を例にした試作版として作成。
- 公式の複数ページで数字を突き合わせて書いた(福岡の部の構成は2ページ一致を確認)。単一の案内にしかない情報(CD販売の詳細、東京の時刻)は、その旨がわかる書き方にしてある。
- 当日の変更(天候・交通の乱れなど)は、公式ブログ・公式Xの「重要なお知らせ」で確認して、速やかに追記する。福岡(2026-09-21)は、台風の影響で、当日、部の構成が変更された(`#change`)。変更前の表には「変更前」と明記して残している。
- 東京握手会(2026-10-04)の公式の発表が更新されたら、`#tokyo` の節を更新する。終わった会場の詳細は、消さずに残し、「開催終了」の帯(`data-event-status`)で当時の情報だと示す。
- 東京は「グループ握手会」の組み合わせと、まとめ出しの部ごとの参加メンバーが公式に出ている。将来は、メンバーを選ぶと、そのメンバーのグループ・部・受付時間がわかる道具にできる。

## ライブ情報(`site/live.html`)
- これから開催されるライブ・イベントを先に載せる。**終わったものは、消さずに「過去の公演」(`past.html`)に残す**(2026-09-22 に、「終わったら消す」から変更)。
- 2026-09-21 に、THREE CONCEPTS LIVE(2026-09-26/27 Kアリーナ横浜)の詳細と、新曲『好きish』の収録曲を掲載。日時・料金・ルールは、公式の3ページで一致を確認した。
- **セットリストの予想は載せない**(ユーザーの方針: 興ざめするため)。実際のセットリストと歌唱メンバーが確定したら、公式・確かな報告で裏を取って、`#setlist` の節に載せる。公演前に「歌われる」とは書かない。
- 過去のライブの雰囲気は、公式の映像(YouTube)を、押すまで読み込まない形(`video.js`、`youtube-nocookie.com`)で載せる。公式チャンネルの映像かと、埋め込み可能かは、YouTube の oEmbed で確認してから足す。
- チケットの販売状況は、日々変わる。特設ページの表示を、確認日つきで書き、「変わる」と明記する。
- 公式ブログなどの本文は、そのまま書き写さない(要点を言い換えて、リンクを付ける)。
- 将来案「東京ドームへの軌跡」: メンバー(倉野尾成美総監督など)の発言は、言い換え+出典リンク+日付で載せる。長い引用や、写真の無断使用はしない。

## ライブレポ・みんなの声(`site/report.html`)
- 行った人の感想を集めるページ。フォームは Netlify Forms(`data-netlify="true"`)。フォーム名は `voice`(感想)と `remove-request`(削除・修正のお願い)。送信の完了ページは `thanks.html`(noindex、sitemap に入れない)。
- 送信されたデータは、CLI で読める: `netlify api listSiteSubmissions --data '{\"site_id\":\"d259c98c-bcac-4871-aa53-92eaa09a9fca\"}'`(PowerShell では、JSON の引用符を `\"` にする)。削除は `deleteSubmission`。
- 載せる流れ: 送信を読む → **ユーザーに、載せてよいものを確認してもらう** → 承諾済み(agree)のものだけを、`site/data/voices.js` に、ニックネームと感想だけ足す → PR → ユーザーがマージ。**メール・IP・本名・SNSのアカウントは、絶対に載せない・チャットにも出しすぎない。**
- **Netlify は、送信ごとに、IPアドレス・User-Agent・参照元も保存する(2026-09-21 に確認)。** そのため、ページ(`report.html` と `about.html`)には、その旨を書いてある。チャットやコミットに、IPを出さない。フォーム検出は、サイト設定 `processing_settings.ignore_html_forms` を `false` にして有効化済み(初期設定は `true` で、フォームが認識されなかった)。
- 載せないもの(ルールはページにも書いてある): 悪口・中傷、個人が特定できる情報、撮影・録音をうかがわせる内容、チケットの売買、宣伝、転載。ネタバレは `spoiler: true`。
- 削除・修正のお願い(`remove-request`)は、優先して対応する。
- Xの投稿は、コピーしない。公式ハッシュタグ(#AKBサンコンコン / #AKBが好きish)の検索へのリンクだけを置く。
- 感想は個人の感想であり、公式の情報ではない。確かな情報(セットリストなど)は、確定したものだけを `live.html#setlist` に載せる。
- 感想フォームの項目(2026-09-22 に改良。フォーム名 `voice`): `show`(どの公演か・必須・1投稿1公演)、`style`(現地 / 配信・必須)、`first_live`(現地の人だけ・必須。今回が初めて / 参加経験あり。「配信」を選ぶと隠れて、無効になり、送信されない)、`fan_years`(ファン歴・任意。1年未満 / 1年以上3年未満 / 3年以上10年未満 / 10年以上)、`returned`(出戻り・任意)、`nickname`、`message`(600文字まで・ヒントと文字数つき)、`spoiler`、`agree`。フォームの選択肢と `voices.js` の文字列は、同じにする(対応表は `voices.js` の先頭)。年齢は聞かない。
  加えて、`prefecture`(どこから来たか・都道府県・任意)と `stayed`(前泊・後泊したか・任意・複数選択可)を 2026-09-22 に追加。この2つは、遠征の案内を考える参考にするだけで、**感想と一緒には載せない**(`voices.js` のスキーマにも入れない)。
- 「誰と行ったか」(`companion`: ひとり / 友達と / 家族と。任意。配信の人は「誰と見たか」)を、2026-09-22 に追加。ラベル(「ひとり参加」「ひとり視聴」など)と、「ひとり参加・視聴」の絞り込みがある。ひとり参加の人がつながる場は、まず安全な形(感想の絞り込み・ひとり参加のガイド)から。チャット・掲示板・待ち合わせの仕組みは、未成年の保護・通報・見守りが必要なので、作らない(作るなら、ユーザーと相談してから)。
- 「ファン歴」と「現地ライブ初参加」は別の質問。以前の `history`(「AKB歴」。「今回が初めて」が混ざっていた)は、新しい項目に**勝手に読み替えない**(意味が確認できないものは、未回答)。掲載する項目(ニックネーム・感想・見た公演・現地か配信か・現地初参加か・ファン歴・出戻り・ネタバレの有無)は、フォームの同意文・ルール・`about.html` の説明と、実際の表示を、必ず一致させる。
- 配信は、Hulu で3公演とも(生配信と見逃し配信。見逃しは10月27日まで、2026-09-22 に Hulu の案内で確認)。ライブビューイングは、公式の案内に出ていない。公演や配信の有無が変わったら、フォームの選択肢と `report.js` の `SHOWS` を、公式に合わせて更新する。
- 一覧は、公演ごとに、現地・配信の順に並べて、「現地」「配信」のラベルで区別する。絞り込みは2段(見た方法 / 現地ライブ初参加・ひとり参加・ファン歴・出戻り)。
- `advice`(次に初めて行く人への、ひと言アドバイス・任意・100文字)を 2026-09-22 に追加。答えがあれば、感想の横に、目立つ枠で表示される(`voices.js` のスキーマにも入れる。公開してよい項目)。`report.html#first` に直接アクセスすると、最初から「現地ライブ初参加」で絞り込んだ状態で開く(`report.js` が URL の hash を見て判定する)。

## ページごとの雰囲気(テーマ)と、写真・映像の方針
- ページごとに `<body class="theme-…">` で色を変える(`style.css` の「ページごとの雰囲気」)。ライブ=むらさき×あお、劇場=あか×きん、握手会・部割プランナー=オレンジ、申し込み=みどり、みんなの声=ピンク×きいろ。各ページの上に、絵文字とひとことの帯(`.page-hero`)がある。色の変数(`--pink` など)を上書きする形なので、追加のページにも同じクラスを付ければよい。
- **写真の方針**: 公式・公演・報道の写真は、無断で載せない(著作権)。載せてよいのは、①ユーザー自身が撮った写真(会場の外観など。公演中の撮影は禁止)、②自由に使える写真(Wikimedia Commons など。ライセンスと出典を必ず書く)、③許可を得た写真、④オリジナルの図。生誕祭の花・劇場の内部の写真は、ファンや公式の撮影物なので特に注意する。
- **映像の方針**: 公式が公開している映像は、YouTube の埋め込み(押すまで読み込まない)で載せる。
- **写真を受け取って載せる手順**(ユーザー自身が撮った写真、または撮った本人が掲載を許可した写真):
  1. 元の写真は、**リポジトリの外の受け取り用フォルダ**で受け取る(GPS・撮影日時などのメタデータを含むので、リポジトリに入れない)。
  2. 載せる前に、撮影者と、サイトへの掲載の許可(もらった写真なら、撮った本人の許可)を確認する。
  3. **載せない**: メンバー本人が写っているもの、お客さんの顔や個人が特定できるもの、公式のポスターや写真が主題のもの、劇場内・公演中に撮ったもの。
  4. **載せる前の処理**: メタデータ(GPS・日時・機種)を削除する(再エンコードで消える)。向きを直す。幅1200px以下に縮小して、軽くする。
  5. `alt` の文章と、撮影者のクレジットを付ける。
  - 劇場の公式Q&Aのルール(2026-09-21 確認): 劇場内は、撮影・録音の持ち込み禁止。ロビーは撮影できるが、メンバーを直接撮るのは禁止。入り待ち・出待ちは禁止。お花・プレゼントは、直接渡さず、ロビーの受付ボックスに預ける。
- **メニュー**: 上のメニューは5つ(ライブ / 握手会 / 劇場公演 / 申し込み / みんなの声)。握手会のグループ(`handshakes.html` `handshake.html` `national.html` `planner.html`)には、タブ(`.subnav`)を付けてある。今いるグループは `class="current"` で強調する。ページを足したら、全ページのメニューと、`sitemap.xml` を更新する。
- **表**: `table.js` が、表の見出しを各セルのラベルにして、`table.stack` を付ける。画面の幅が820px以下だと、表は「カード」の形になる(スマホで、文字が縦に並ぶのを防ぐ)。表を足したページには `<script src="table.js"></script>` を入れる。

## ペンライトカラー(`site/penlight.html`)と持ち物ガイド(`site/live-guide.html`)
- 全45名のペンライトカラー。データは `site/data/penlight.js`。出典は2種類: 公式の一覧(2022年10月5日・26名)と、ファン有志の一覧を参考に運営者が確認したもの(2022年以降に加入した19名)。**2022年の公式の色は、今は変わっている可能性がある**と、ページに書いてある。メンバーが増減したら、`members` を更新する。
- 色の修正のお知らせは、Netlify Forms の `correction` フォームで受ける(フォーム名 `correction`)。内容は、運営者が確認してから直す。送信には、IP等も保存される。
- **出演メンバーだけ表示の絞り込み**(2026-09-22): `PENLIGHT.eventCasts["ライブのID"]` に、そのライブの出演者名(公式の特設ページと1文字ずつ照合したもの)を書くと、ページに絞り込みチェックボックスが出る。**22期研究生のように、名前がまだ公式に発表されていない人は、`eventCasts` にも `members` にも入れない**(推測で埋めない)。ライブが終わったら、この絞り込みは残しておいてよい(次のライブのIDに書き換えるか、複数を残すかは、そのとき判断する)。
- 持ち物ガイドは、情報を「公式」「ファンの声」「このサイトの提案」のラベルで分ける(`.ex` `.ex.fan` `.ex.mine`)。ファンのガイドは、要点だけを言い換えて、出典を書く。会場(Kアリーナ横浜)のルールは、公演ごとに主催者が決めるので、AKB48の案内が優先と書く。
- 持ち物チェックリストは、`localStorage`(この端末だけ)に保存する。運営者には、送られない。

## 劇場公演(`site/theater.html`)の更新の方針
- 料金は、2026年4月8日の公演から改定(通常4,300円・研究生公演4,100円・卒業セレモニー公演5,300円・特別公演6,300円、ロビー観覧チケット1,800円)。公式のお知らせ(2026年4月)が根拠。料金が変わったら、公式のお知らせで確認して更新する。
- ガラポンチャレンジ(2026年4月7日の公式のお知らせ): 1枚500円、開演2時間前〜45分前、1会計4枚まで。当たり①=2ショットチェキ(出演メンバーを1名指名)、当たり②=お見送りでスマホ撮影。「赤玉」「緑玉」という呼び方は、ファンの体験記にあった呼び方で、公式の案内には出ていないので、そう書き分けている。
- **「繰り上がり」(キャンセル待ち)の仕組みは、公式のページで確認できていない**ため、載せていない。確認できたら足す(ページに「確認でき次第、追記」と書いてある)。

## 情報の扱い
- ルールや申し込み方法は変わる。必ず公式の原文で確認し、各ページに確認日と公式リンクを載せる。
- 部割は公式のスケジュール(PDF・画像)を読み取ったもの。誤読の恐れがあるので、公開前にユーザーに確認してもらう。
- 公式の写真・ロゴ・歌詞は載せない。「非公式」と明記する。

## 公開の運用ルール(2026-09-22 に変更。Netlify無料枠の節約のため)
公開先は Netlify。GitHub リポジトリ(https://github.com/puriketty/akb-hajimete-guide)と連携済み。
プロジェクト名 `akb-hajimete-guide`(サイトID `d259c98c-bcac-4871-aa53-92eaa09a9fca`)、公開URL: https://akb-hajimete-guide.netlify.app

- **本番デプロイ(`main` へのマージ)は、Netlify無料枠で月20回まで。回数を節約する。** 1つずつマージせず、まとめて溜めてから、まとめてマージする。
- **`main` には直接デプロイしない。`netlify deploy --prod` も使わない。** 本番を動かす方法は、`main` へのマージだけ。
- **作業はすべて、作業用ブランチ `dev` の上で行う。** 個別の `fix/…` `feat/…` ブランチは、もう作らない。`dev` から `main` への PR を1つ持ち続け(なければ作る)、直した内容は、その PR に push で積み重ねる。
- 確認は、その PR の**デプロイプレビュー**(`https://deploy-preview-<PR番号>--akb-hajimete-guide.netlify.app/`)で行う。プレビューは無料枠の本番回数を消費しない。
- **`main` へのマージ(=本番への反映)は、ぱちゃが「マージしてOK」と、はっきり言ったときだけ行う。** それ以外(「マージ完了」などユーザー自身がGitHub上で操作した場合を除く)は、Claude からは `main` に触れない。「マージしてOK」をもらったら、`dev` を `main` に(通常どおり squash で)マージし、本番デプロイの結果を確認してから、Notionの開発ログとチャットに報告する。
- 公開するのは `netlify.toml` で指定した `site/` だけ。`CLAUDE.md` `README.md` はサイトとしては公開されない。**ただし、`main` に push すると、中身に関わらず本番のビルドが1回動こうとする**(2026-09-22 に確認。`CLAUDE.md` だけの変更でも、本番デプロイの試行が記録された)。**そのため、`CLAUDE.md` や `README.md` だけの変更も、必ず `dev` 経由にする。**`main` に直接コミットしない。
- 「公開して」と言われたら、`dev` で作業して push し、デプロイプレビューを確認してから、ぱちゃに報告する(「マージしてOK」をもらうまでは、マージしない)。
- マージ後は、URL をブラウザで開いて、表示と部割プランナーの動きを確認する。

### 注意
- **このリポジトリは公開(PUBLIC)。** `CLAUDE.md` も誰でも読める。鍵・パスワード・トークン・個人のメールアドレス・パソコン内の個人フォルダの場所は書かない。
- 非公開にしていたときは、Netlify の無料プランが「非公開リポジトリのビルドは確認済みメンバーのみ」と判断してビルドを止めた(Unrecognized Git contributor)。非公開に戻すと、同じ問題が出る可能性がある。
- git と gh は、winget で入れた直後のセッションでは PATH を読み直す:
  `$env:Path = [Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [Environment]::GetEnvironmentVariable("Path","User")`
- 新しい Netlify プロジェクトは初期設定が非公開(Team protection)。公開の切り替え(Project configuration > General > Visitor access > Project visibility)は、セキュリティ設定なのでユーザー本人にやってもらう。
